class Module (var NomModule: String, var Coeff: Int){
def getNomModule(): String = {
return NomModule
}
def setNomModule(Nom : String): Unit = {
NomModule = Nom
}
def getCoeff(): Int = {
return Coeff
}
def setCoeff(c : Int): Unit = {
Coeff = c
}
}